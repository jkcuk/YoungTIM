package javawaveoptics.utility;

/**
 * Defines a class which represents an optical component which is a source of light from an imageable
 * component - that is to say, a component whose class implements the Imageable interface.
 * 
 * This is useful so that the program can identify components that form part of a resonator, and so provide
 * useful functionality such as 'Simulate 100 times' buttons.
 * 
 * @author Sean
 */
public interface ImageableLightSourceInterface
{

}
