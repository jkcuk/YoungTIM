/* */package library.list;import java.awt.*;import java.io.*;public class DoubleArray2D extends DoubleList2DClassimplements Serializable{	private static final long serialVersionUID = 6257524110487252290L;		private int width, height;	private double data[][];		// this constructor simply reserves space for <width>*<height> double numbers, which	// can be given values using the method setElement(int i, int j, double c)	public DoubleArray2D(int width, int height)	{		this.width = width;		this.height = height;				// allocate memory		data = new double[width][height];	}		public DoubleArray2D(Dimension size)	{		this.width = size.width;		this.height = size.height;				// allocate memory		data = new double[width][height];	}		// this constructor COPIES the DoubleList2D d into its	// private array	public DoubleArray2D(DoubleList2D d)	{		width = d.getSize().width;		height = d.getSize().height;		data = new double[width][height];				for(int i=0; i<width; i++)			for(int j=0; j<height; j++)				data[i][j] = d.getElement(i, j);	}		// this constructor 'wraps' a DoubleArray2D around the 2-dimensional array of doubles <d>	public DoubleArray2D(double[][] d)	{		width = d.length;		height = d[0].length;				data = d;	}		public Dimension getSize()	{		return new Dimension(width, height);	}		public double getElement(int i, int j)	{		return data[i][j];	}		public void setElement(int i, int j, double value)	{		data[i][j] = value;	}		public void setRow(int j, double row[])	{		// the array rowValues has to be of length width		for(int i=0; i<width; i++)			setElement(i, j, row[i]);	}		public void setRow(int j, DoubleList1D row)	{		// the array rowValues has to be of length width		for(int i=0; i<width; i++)			setElement(i, j, row.getElement(i));	}		public void setColumn(int i, double column[])	{		// the array columnValues has to be of length height		for(int j=0; j<height; j++)			setElement(i, j, column[j]);	}	public void setColumn(int i, DoubleList1D column)	{		// the array columnValues has to be of length height		for(int j=0; j<height; j++)			setElement(i, j, column.getElement(j));	}}